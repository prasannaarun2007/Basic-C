#include <stdio.h>
int main()
{
    int n,i,q,rem,rev=0;
    printf("ENTER YOUR NUMBER:-");
    scanf("%d",&n);
    printf("*****************\n");
    for(i=1;n>0;i++)
    {
        rem=n%10;
        rev=(rev*10)+rem;
        q=n/10;
        n=q;
    }
    printf("REVERSE NUMBER IS=%d",rev);
}
